# 服务名称
spring:
  application:
    name: beacon-smsgateway
  # 多环境
  profiles:
    active: dev
  # nacos注册中心地址
  cloud:
    nacos:
      discovery:
        server-addr: 124.71.222.156:8848
      # nacos配置中心地址:
      config:
        server-addr: 124.71.222.156:8848
        file-extension: yml